# Dependencies
- `zsh`
- `wget`
- `git`

# Quick Install
```
cd ~ && git clone https://github.com/zsh-users/zsh-autosuggestions --depth=1 ~/.zsh && rm -rf ~/.zsh/zsh-autosuggestion/.git &&git clone https://github.com/zsh-users/zsh-history-substring-search --depth=1 ~/.zsh && rm -rf ~/.zsh/zsh-history-substring-search/.git && git clone https://github.com/zsh-users/zsh-syntax-highlighting --depth=1 && rm -rf ~/.zsh/zsh-syntax-highlighting/.git && wget https://raw.githubusercontent.com/ayumodark/zshrc/refs/heads/config/.zshrc && exec zsh
```
